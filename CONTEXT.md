# 供应商价格与路由

本上下文描述供应商、访问分组、凭据、价格推荐与api路由之间的统一业务语言。

## Language

**供应商配置**：
本工具中一个外部供应商的唯一业务记录；价格查询、访问分组、 API key 和路由都归属于它，同一供应商不建立第二个账号。
_Avoid_: 站点、线路、Provider Profile、供应商账号

**访问分组**：
供应商配置下决定模型权限与计费条件的稳定业务身份；它与 API key 存在可变绑定，不以 key 原文作为身份。
_Avoid_: API key、账号、倍率

**API key**：
授权模型 API 调用的可替换秘密；属于一个供应商配置，并在任一时刻绑定一个访问分组。
_Avoid_: 站点访问凭据、登录凭据、访问分组

**API key 摘要**：
用于辨认已保存 API key 的非秘密表示；长度至少 12 时显示前 4 位与后 4 位，较短时统一显示固定数量星号且不暴露实际长度。
_Avoid_: API key 原文、可逆掩码

**站点访问凭据**：
授权供应商网站登录态或价格数据查询的秘密，例如 AccessToken 或 Cookie；不授权模型推理，也不与访问分组绑定。
_Avoid_:  API key、访问分组

**分组绑定**：
 API key 与一个访问分组之间的可变关联；用户在供应商侧改组后，由本工具显式更新。
_Avoid_: 永久归属、自动换组、站点登录态

**活动供应商**：
所有正在运行的 OMP 实例共享的可选推理目标；只有用户显式应用供应商时设置，删除其 API key 时清除且不自动回退。
_Avoid_: 每实例供应商、自动推荐目标、无 key 的残留目标
**OMP 配置替换**：
用户明确发起的配置迁移动作；先选择目标 OMP Provider，只改变 GPT 系列路由模型所关联的 Provider，保留非 GPT 路由及其原供应商。目标为工具维护的本地 Provider 时，同时维护其 Provider 定义；目标为官方 OAuth Provider 时，不修改 Provider 定义。它不改变活动供应商，也不表示 OMP 已进入某种持久接管状态。
_Avoid_: 首次接管状态、自动替换、全量替换、活动供应商切换



**路由快照**：
一次推理请求开始时捕获的活动供应商；该请求在完成或失败前保持此目标，之后的新请求使用最新活动供应商。
_Avoid_: 请求中途切换、全局可变引用
**GPT 路由模型**：
路由引用中 `/` 后面的模型标识以 `gpt` 开头的模型，使用不区分大小写的序数匹配；只有这类模型允许被 OMP 配置替换动作改写。包含 `deepseek`、`claude` 或其他非 `gpt` 前缀的模型保持原 Provider 和原引用。
_Avoid_: 模型名任意包含 `gpt`、全量模型替换、按 Provider 名称判断

**配置替换预览**：
用户点击 OMP 配置替换后，系统先展示将被改写的 GPT 路由角色、原 Provider、目标 Provider 和模型标识；用户明确确认后才写入配置。取消预览不产生文件或路由副作用。
_Avoid_: 点击即写入、隐藏式替换、把非 GPT 路由列为变更
配置替换预览只列出实际会改变的 GPT 路由；已使用目标 Provider 的 GPT 路由和所有非 GPT 路由不列入变更。没有可变更项时只显示无变更结果，不写入文件。
配置替换只处理 OMP 主 `config.yml` 中约定的模型角色引用；不递归扫描或改写其他 YAML。`models.yml` 仅在目标为 `provider-price-switcher` 时维护该 Provider 定义。
**配置替换生效**：
配置替换只改变磁盘上的 OMP 配置；已运行的 OMP 实例和在途请求保持不变，用户需要手动重启 OMP 后，新配置才对该实例生效。
**配置替换入口**：
首页 OMP 操作区域中的独立用户动作；用户选择 `provider-price-switcher` 或 `openai-codex` 后先预览 GPT 路由变更，确认后写入配置，不依赖接管状态，不启动 OMP，也不应用活动供应商。







**当前绑定分组**：
当前唯一 API key 实际绑定的访问分组；本版本唯一可被用户选择并应用的分组，其他价格分组只读展示。
_Avoid_: 最低价分组、推荐分组（当它未绑定当前 key 时）

**价格基准模型**：
用于计算和比较供应商成本的代表模型；当前版本使用 GPT 系列的单一基准，未来可扩展为多个模型类型。
_Avoid_: 唯一可调用模型、路由模型别名

**路由模型**：
OMP 在推理请求中指定并由网关原样转发的具体 GPT ModelId；它不由价格基准模型替换。
_Avoid_: 价格基准模型、自动模型 fallback

**模型发现**：
OMP 向固定本地 Provider 查询模型列表时，由 sidecar 对每次请求捕获当前活动供应商并实时转发；结果只属于该路由，不聚合、不缓存，也不建立同名模型的跨供应商路由。
_Avoid_: 静态模型声明、全供应商模型目录、模型 fallback
